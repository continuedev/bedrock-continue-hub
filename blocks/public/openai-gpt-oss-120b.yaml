name: GPT-OSS 120B
version: 1.0.0
schema: v1

models:
  - name: GPT-OSS 120B
    provider: bedrock
    model: openai.gpt-oss-120b-1:0
    env:
      region: ${{ inputs.BEDROCK_AWS_REGION }}
      profile: ${{ inputs.BEDROCK_AWS_PROFILE }}
    roles:
      - chat
      - apply
      - edit