name: claude v2:0:100k
version: 1.0.4
schema: v1

models:
  - name: claude v2:0:100k
    provider: bedrock
    model: anthropic.claude-v2:0:100k
    env:
      region: ${{ inputs.BEDROCK_AWS_REGION }}
      profile: ${{ inputs.BEDROCK_AWS_PROFILE }}
    roles:
      - chat
      - apply
      - edit
    capabilities:
      - tool_use
    defaultCompletionOptions:
      contextLength: 100000